---
import BaseLayout from '../../layouts/BaseLayout.astro';

const target = `${import.meta.env.BASE_URL}#publications`;
---

<BaseLayout title="Publications" description="Redirecting to the Publications section.">
  <Fragment slot="head">
    <meta http-equiv="refresh" content={`0;url=${target}`} />
  </Fragment>
  <section class="site-shell section">
    <div class="stack">
      <h1 class="hero__heading" style="font-size: clamp(2rem, 1.8rem + 1vw, 2.6rem);">
        Redirecting to Publications
      </h1>
      <p class="prose" style="max-width: 52ch;">
        Publications now live on the homepage. Follow the link below if you are not redirected.
      </p>
      <div class="hero__actions">
        <a class="button" href={target}>Go to Publications</a>
      </div>
    </div>
  </section>
  <script is:inline>
    window.requestAnimationFrame(() => window.location.replace(target));
  </script>
</BaseLayout>

